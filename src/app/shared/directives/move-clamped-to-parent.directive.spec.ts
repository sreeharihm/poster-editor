import { MoveClampedToParentDirective } from './move-clamped-to-parent.directive';

describe('MoveClampedToParentDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveClampedToParentDirective();
    expect(directive).toBeTruthy();
  });
});
